import { grayColor, whiteColor } from "../../constants/colors";

export default () => ({
  postContainer: {
    padding: "0 0.2rem",
    height: "40%",
  },
  image: {
    width: "100%",
    height: "100%",
    objectFit: "cover"
  }
});
