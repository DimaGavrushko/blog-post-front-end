import { whiteColor } from "../../../constants/colors";

export default () => ({
  logoContainer: {
    display: "flex",
    color: whiteColor,
    fontSize: 32,
    marginRight: "4rem",
    padding: "0.3rem 0",
    textDecoration: "none"
  },
  bold: {
    fontWeight: "bold"
  }
});
